The agent is allowed to:

* Read project files
* Modify code
* Create new components
* Improve SEO metadata
* Add schema markup
* Optimize performance
* Suggest UI improvements

The agent must not:

* Delete important project directories
* Modify environment secrets
* Change deployment configuration without explanation
